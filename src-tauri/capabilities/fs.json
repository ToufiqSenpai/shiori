{
  "$schema": "./default.json",
  "identifier": "fs",
  "description": "File system access capability",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "fs:allow-write-file",
      "allow": [{ "path": "**/*.pdf" }]
    }
  ]
}
